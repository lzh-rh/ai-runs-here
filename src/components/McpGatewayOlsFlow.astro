<figure class="mcp-flow" aria-labelledby="mcp-flow-title" aria-describedby="mcp-flow-description">
  <figcaption id="mcp-flow-title">What you will build</figcaption>
  <p id="mcp-flow-description" class="mcp-flow__intro">
    A connection for listing Pods from OpenShift Lightspeed.
  </p>

  <ol class="mcp-flow__overview" role="list" aria-label="Read-only tool call from Lightspeed to the OpenShift API">
    <li class="mcp-flow__node mcp-flow__node--client">
      <strong>OpenShift Lightspeed</strong>
      <small>Requests a tool to list Pods</small>
      <span class="mcp-flow__arrow" aria-hidden="true">↓</span>
    </li>
    <li class="mcp-flow__node mcp-flow__node--gateway">
      <strong>MCP Gateway</strong>
      <small>Routes the call to the MCP server</small>
      <span class="mcp-flow__arrow" aria-hidden="true">↓</span>
    </li>
    <li class="mcp-flow__node">
      <strong>OpenShift MCP server</strong>
      <small>Runs the tool and calls the OpenShift API</small>
      <span class="mcp-flow__arrow" aria-hidden="true">↓</span>
    </li>
    <li class="mcp-flow__node mcp-flow__node--api">
      <strong>OpenShift API</strong>
      <small>Checks permissions and returns permitted Pod data</small>
    </li>
  </ol>
</figure>

<style>
  .mcp-flow {
    margin: 2rem 0;
    padding: clamp(1rem, 3vw, 1.6rem);
    border: 1px solid var(--rule);
    border-top: 5px solid var(--rh-red);
    border-radius: 0.75rem;
    background: var(--paper);
  }

  figcaption {
    color: var(--ink);
    font-family: var(--font-heading);
    font-size: clamp(1.35rem, 3vw, 1.8rem);
    font-weight: 700;
    line-height: 1.2;
  }

  .mcp-flow__intro {
    margin: 0.55rem 0 1.25rem;
    color: var(--muted);
  }

  .mcp-flow__overview {
    display: grid;
    gap: 1.8rem;
    max-width: 30rem;
    margin: 0 auto;
    padding: 0;
    list-style: none;
  }

  .mcp-flow__node {
    position: relative;
    display: grid;
    gap: 0.2rem;
    min-width: 0;
    margin: 0;
    padding: 0.7rem 1rem;
    border: 1px solid var(--rule);
    border-radius: 0.55rem;
    background: var(--paper);
    text-align: center;
  }

  .mcp-flow__node--client {
    border-color: var(--link);
    background: var(--blue-soft);
  }

  .mcp-flow__node--gateway {
    border: 2px solid var(--rh-red-dark);
  }

  .mcp-flow__node--api {
    border-color: var(--gold);
    background: var(--gold-soft);
  }

  .mcp-flow__node strong {
    color: var(--ink);
    font-size: 1.05rem;
    line-height: 1.35;
  }

  .mcp-flow__node small {
    color: var(--muted);
    font-size: 0.9rem;
    line-height: 1.4;
  }

  .mcp-flow__arrow {
    position: absolute;
    top: 100%;
    left: 50%;
    display: grid;
    height: 1.8rem;
    place-items: center;
    transform: translateX(-50%);
    color: var(--rh-red-dark);
    font-size: 1.4rem;
    line-height: 1;
  }
</style>
